output_dir: './output'
cache_dir: './cache'
experiment_templates:
  - name: 'invalid'
    seed: 'invalid'  # Invalid data type for seed
    device: 'gpu'  # Invalid device name
    model:
      name: 'invalid'  # Invalid model name
    dataset:
      name: 'invalid'  # Invalid dataset name
    attack:
      scope: 'invalid'  # Invalid attack scope
      type: 'invalid'  # Invalid attack type
      name: 'invalid'  # Invalid attack name
      epsilon: 2.0  # Epsilon out of range
    training:
      max_epochs: 10
      patience: 20  # Patience greater than max_epochs
    optimizer:
      name: 'invalid'  # Invalid optimizer name
    loss:
      name: 'invalid'  # Invalid loss name